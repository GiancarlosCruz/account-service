spring:
  application:
    name: accountservice
  config:
    import: "optional:configserver:http://localhost:8888"
  cloud:
    config:
      fail-fast: false
      import-check:
        enabled: false